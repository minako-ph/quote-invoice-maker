# 第三者レビュー: jp-business-api 実装初日（Step 0〜6）

**2026-07-11 / レビュアー: チャット側Claude / 対象: master `cab9b99`（git clone による全ファイル読込＋検証コマンド実走）**
**配置先: docs/review-2026-07-11.md**

## 結論

**承認**（指摘は軽微2件・いずれも文書修正のみ、コード変更なし）。F-1/F-2の適用をもって requirements §8 のDone定義は全達成とみなし、人間タスク（デプロイ・RapidAPI登録）へ進んでよい。

## 検証結果（全項目パス）

- **検証コマンド実走**: `pnpm typecheck` / `pnpm lint` / `pnpm test` すべて緑、**146テスト全パス**（レビュー環境: Node 22.22.2 / pnpm 11.10.0 / frozen-lockfile）。fail-close時の403ログもテスト中に確認
- **N-2**: `wrangler deploy --dry-run` 実測 **693.87 KiB（gzip 109.37 KiB）** — 3MB制約内
- **CR-1 / CR-5**: src・scripts・openapi に照会系（法人番号・gBizINFO・インボイス・EDINET）・日本郵便APIのキーワードなし（grep検証）
- **CR-4**: 全git履歴（unshallow後）にシークレット値・.dev.vars の混入なし。.gitignore 適切、.dev.vars.example はダミー値のみ
- **N-1**: src/ に実行時 fetch ゼロ（祝日データ・元号表はビルド時同梱のみ）
- **FR-11**: 定数時間比較（長さ差でも全バイト走査）・fail-close・スキップ手段なし・`app.use('*')` で全ルート適用を目視確認
- **FR-10**: covered_from/to は生成データから機械決定。年単位判断（〜2027-12-31）は decisions.md 記録済みで妥当（CSVは年単位追記のため2027年分は完全収録）
- **FR-3境界**: 明治1868-10-23 / 大正1912-07-30 / 昭和1926-12-25 / 平成1989-01-08 / 令和2019-05-01、1873-01-01下限、era_date_mismatch 検証を目視確認。golden で境界8点＋範囲外エラーを固定済み
- **N-3**: golden 35件が要求全点を網羅（元号境界5点・振替休日・うるう年・年末年始・収録範囲両端・weekendsカスタム・extra_holidays・FR-13超過3種）
- **CR-2**: data/README.md（出典URL・CC-BY・取得日・年次更新手順）＋ README の Data source 節
- **CR-3**: README "What this API does NOT do" 5項目（照会系なし・収録範囲・1873年・extra_holidays・政令変更）
- **FR-14**: OpenAPI 8パス網羅（test/openapi.test.ts でガード）
- **規約**: AGENTS.md/CLAUDE.md symlink、SYNC.md（取込ハッシュ・再取込手順・読み取り専用規律）、decisions.md 13件の判断ログ

## 指摘（F-x形式。いずれも軽微・文書のみ）

### F-1【軽微】decisions.md のテスト数記録が古い

- 対象: docs/decisions.md 最上行「typecheck/lint/test緑（**143テスト**）」
- 事実: 実測は **146テスト**（7ファイル）。完了報告の146が正
- 修正: 既存行は書き換えず（ログの追記主義）、新規1行を最上部に追記する。文言例:
  `- 2026-07-11: 第三者レビュー承認（対象 cab9b99、レビューはgit clone＋検証コマンド実走）。テスト実数は146（既存記載143は最終記録時点の値）。バンドル実測693.87KiB/gzip109KiB`

### F-2【軽微】requirements FR-1 受入基準の祝日名称を実データに合わせる

- 対象: docs/requirements.md FR-1 受入基準「2019-05-01（令和元年初日・祝日「休日」）」
- 事実: CSV実名称は「**休日（祝日扱い）**」（decisions.md 記録済み。golden はCSV原文を正としており実装側は正しい）
- 修正: 当該箇所を「祝日「休日（祝日扱い）」」へ1箇所置換（原則: 実データ＞文書、差分は文書へ反映）

## 適用手順（Claude Code向け一言）

> docs/review-2026-07-11.md を読んで F-1 / F-2 を適用して。コード変更なし・文書2箇所のみ。適用後 `pnpm typecheck && pnpm lint && pnpm test` の緑を確認してコミット。
